# ============================================================
# Portail des stages — variables d'environnement (backend)
# Copiez, adaptez, exportez. Ne jamais committer de secrets réels.
# ============================================================
DB_URL=jdbc:postgresql://localhost:5432/internship_management
DB_USERNAME=internship_user
DB_PASSWORD=changeme

# JWT (openssl rand -base64 48 en production)
JWT_SECRET=devOnlyChangeMeReplaceInProduction
JWT_EXPIRATION=86400000

# E-mail (Gmail : utilisez un mot de passe d'application)
MAIL_HOST=smtp.gmail.com
MAIL_USERNAME=
MAIL_PASSWORD=
